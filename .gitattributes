# Keep LF in the working tree on every platform. server.js is an executable
# with a `#!/usr/bin/env node` shebang: published with CRLF it fails on
# macOS/Linux with "env: node\r: No such file or directory".
* text=auto eol=lf
*.png binary
*.gif binary
